import 'package:flutter/material.dart';

class GroubsPage extends StatelessWidget {
  const GroubsPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}